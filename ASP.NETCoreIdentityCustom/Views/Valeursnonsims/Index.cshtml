@model IEnumerable<Rappro.Data.Valeursnonsim>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FichierVns)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumRappNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FichierVns)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumRappNavigation.NumRapp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idvns">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idvns">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idvns">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
