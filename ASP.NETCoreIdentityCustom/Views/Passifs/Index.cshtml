@model IEnumerable<Rappro.Data.Passif>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FichierPassif)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FichierPassif)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumPassif">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumPassif">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumPassif">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
